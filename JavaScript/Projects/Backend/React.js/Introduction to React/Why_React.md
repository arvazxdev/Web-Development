# Why React?

* States allow updates that automatically reflect throughout the page
* The app can be divided into several components, enabling component reuse
* React employs a virtual DOM to optimize UI updates, outperforming direct DOM manipulation
* Debugging and maintenance processes are straightforward
